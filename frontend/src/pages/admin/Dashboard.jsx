import { useEffect, useState } from "react";
import axiosClient from "../../api/axiosClient";
import { formatCurrency, formatDate, orderStatusLabel } from "../../utils/formatters";
export default function Dashboard() { const [orders, setOrders] = useState([]); const [error, setError] = useState(""); useEffect(() => { axiosClient.get("/admin/orders", { params: { limit: 5 }, sessionProtected: true }).then(({ data }) => setOrders(data.data.orders)).catch((requestError) => setError(requestError.response?.data?.message || "Không thể tải đơn hàng.")); }, []); return <><div className="stat-grid"><article><div><small>Thống kê tổng hợp</small><strong>Chưa có API</strong><em>Không sử dụng số liệu giả</em></div></article></div><section className="admin-table-card"><div className="table-title"><h3>Đơn hàng gần đây</h3></div>{error ? <div className="empty">{error}</div> : orders.length ? <table><thead><tr><th>Mã đơn</th><th>Khách hàng</th><th>Ngày đặt</th><th>Tổng tiền</th><th>Trạng thái</th></tr></thead><tbody>{orders.map((order) => <tr key={order._id}><td>#{order._id.slice(-8).toUpperCase()}</td><td>{order.user?.fullName || "—"}</td><td>{formatDate(order.createdAt)}</td><td>{formatCurrency(order.total)}</td><td>{orderStatusLabel[order.orderStatus]}</td></tr>)}</tbody></table> : <div className="empty">Chưa có đơn hàng.</div>}</section></>; }
